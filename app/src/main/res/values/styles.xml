<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">

    </style>

    <!-- Splash Screen theme. -->
    <style name="Splash.Theme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/bg_splash</item>
    </style>

    <!-- Red Gradient theme. -->
    <style name="Red.Gradient.Theme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/bg_red_gradient</item>
    </style>

</resources>